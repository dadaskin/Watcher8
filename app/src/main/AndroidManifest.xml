<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.adaskin.android.watcher8">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.OwnedDetailsActivity" android:label="@string/app_name"/>
        <activity android:name=".views.OwnedAddActivity" android:label="@string/app_name"/>
        <activity android:name=".views.WatchDetailsActivity" android:label="@string/app_name"/>
        <activity android:name=".views.WatchAddActivity" android:label="@string/app_name"/>
        <activity android:name=".views.ChangeParameterActivity" android:label="@string/app_name"/>
        <activity android:name=".views.BuyBlockActivity" android:label="@string/app_name"/>

    </application>

</manifest>